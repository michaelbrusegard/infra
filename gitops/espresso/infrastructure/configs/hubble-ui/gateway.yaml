apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: hubble-ui
  namespace: kube-system
  annotations:
    io.cilium/lb-ipam-ips: "10.0.187.128,fd7a:115c:a1e0:188::1"
spec:
  gatewayClassName: cilium
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same
