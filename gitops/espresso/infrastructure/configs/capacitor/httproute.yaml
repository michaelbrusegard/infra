apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: capacitor
  namespace: flux-system
spec:
  parentRefs:
    - name: capacitor
      namespace: flux-system
  rules:
    - backendRefs:
        - name: capacitor
          port: 80
