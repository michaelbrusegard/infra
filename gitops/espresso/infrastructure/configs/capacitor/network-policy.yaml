apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-capacitor-gateway
  namespace: flux-system
spec:
  podSelector:
    matchLabels:
      app: capacitor
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 9000
          protocol: TCP
