apiVersion: v1
kind: ConfigMap
metadata:
  name: stalwart-edge-tofu-provider
  namespace: flux-system
data:
  tofurc: |
    provider_installation {
      filesystem_mirror {
        path    = "/provider-mirror"
        include = ["registry.opentofu.org/tahacodes/stalwart"]
      }
      direct {
        exclude = ["registry.opentofu.org/tahacodes/stalwart"]
      }
    }
