apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: nix-secrets
  namespace: flux-system
spec:
  interval: 10m
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/michaelbrusegard/nix-secrets
