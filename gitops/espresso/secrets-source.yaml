apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: infra-secrets
  namespace: flux-system
spec:
  interval: 10m
  ref:
    branch: main
  secretRef:
    name: secrets-auth
  url: ssh://git@github.com/michaelbrusegard/infra-secrets
