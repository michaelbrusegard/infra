apiVersion: v1
kind: ConfigMap
metadata:
  name: hermes-agent-bootstrap
  namespace: hermes-agent
data:
  config.yaml: |
    model:
      provider: zai
      default: glm-5

    terminal:
      backend: local
      cwd: /opt/data/workspace
