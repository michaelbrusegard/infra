apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: immich-internal
  namespace: immich
spec:
  parentRefs:
    - name: internal
      namespace: gateway
      sectionName: https
  hostnames:
    - photos.asgard.michaelbrusegard.com
  rules:
    - backendRefs:
        - name: immich
          port: 80
