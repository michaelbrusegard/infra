--- /dev/null
+++ b/gitops/espresso/tofu/stalwart-edge/filtering.tf
@@ -0,0 +1,6 @@
+resource "stalwart_sieve_system_script" "auth_verdict" {
+  name        = "edge-auth-verdict"
+  description = "Replace untrusted authentication headers with native verdicts for private backend scoring"
+  is_active   = true
+  contents    = trimspace(file("${path.module}/../../apps/stalwart-edge/auth-verdict.sieve"))
+}
--- a/gitops/espresso/tofu/stalwart-edge/ingress.tf
+++ b/gitops/espresso/tofu/stalwart-edge/ingress.tf
@@ -68,6 +68,7 @@
 
 resource "stalwart_mta_stage_data" "edge" {
   enable_spam_filter = { else = "local_port == 25", match = [] }
+  script             = { else = "'${stalwart_sieve_system_script.auth_verdict.name}'", match = [] }
 }
 
 resource "stalwart_sender_auth" "edge" {
