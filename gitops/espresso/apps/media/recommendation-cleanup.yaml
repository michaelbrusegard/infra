apiVersion: batch/v1
kind: CronJob
metadata:
  name: recommendation-cleanup
  namespace: media
spec:
  schedule: "10 5 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        metadata:
          labels:
            app: recommendation-cleanup
        spec:
          restartPolicy: Never
          nodeSelector:
            kubernetes.io/hostname: espresso-1
          containers:
            - name: cleanup
              image: docker.io/library/python:3.14-slim@sha256:8e4071294d046d45b31a902e02a8560a45c351898513b66ec659ca39fd30d170
              command:
                - python
                - /scripts/cleanup.py
              env:
                - name: MUSIC_ROOT
                  valueFrom:
                    configMapKeyRef:
                      name: listenbrainz-bridge-config
                      key: MUSIC_ROOT
                - name: STATE_DB
                  valueFrom:
                    configMapKeyRef:
                      name: listenbrainz-bridge-config
                      key: STATE_DB
                - name: CLEANUP_AFTER_DAYS
                  valueFrom:
                    configMapKeyRef:
                      name: listenbrainz-bridge-config
                      key: CLEANUP_AFTER_DAYS
                - name: NAVIDROME_DB
                  value: /navidrome/navidrome.db
              resources:
                requests:
                  cpu: 100m
                  memory: 256Mi
                limits:
                  cpu: "1"
                  memory: 512Mi
              volumeMounts:
                - name: media-data
                  mountPath: /music
                  subPath: media/music
                - name: state
                  mountPath: /state
                - name: navidrome-data
                  mountPath: /navidrome
                  readOnly: true
                - name: scripts
                  mountPath: /scripts/cleanup.py
                  subPath: cleanup.py
          volumes:
            - name: media-data
              persistentVolumeClaim:
                claimName: media-data
            - name: state
              persistentVolumeClaim:
                claimName: config-listenbrainz-bridge
            - name: navidrome-data
              persistentVolumeClaim:
                claimName: data-navidrome-0
                readOnly: true
            - name: scripts
              configMap:
                name: listenbrainz-bridge-config
                defaultMode: 0755
