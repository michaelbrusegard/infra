apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Staged only: do not add to apps/kustomization.yaml before the README gates pass.
resources:
  - namespace.yaml
  - statefulset.yaml
  - services.yaml
  - network-policy.yaml
  - backup.yaml
