apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: manafishrov
  namespace: flux-system
spec:
  interval: 10m
  ref:
    branch: main
  secretRef:
    name: manafishrov-auth
  url: ssh://git@github.com/manafishrov/infra
