For every human-facing artifact written on Michael's behalf, include model
provenance exactly as required by AGENTS.md. Use the runtime's exact model slug
or UNKNOWN-MODEL; never guess. Verify the attribution immediately before any
external write.
